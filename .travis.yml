language: java

jdk:
  - oraclejdk8

services:
  - mysql

before-install:
#  - mysql -e "grant all privileges on belajar.* to 'root'@'localhost'"
#  - mysql -e "drop database if exist belajar"
  - mysql -e "create database if not exists belajar"